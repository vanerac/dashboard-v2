name: Push CI
on: push

jobs:
  install-dependencies:
    name: Install dependencies
    uses: vanerac/dashboard-v2/.github/workflows/install-dependencies.yml@${{GITHUB_REF##*/}}
#  lint:
#    name: Lint
#    needs: install-dependencies
#    uses: vanerac/dashboard-v2/.github/workflows/lint.yml
#  prettier:
#    name: Prettier
#    needs: install-dependencies
#    uses: vanerac/dashboard-v2/.github/workflows/prettier.yml
#  openapi:
#    name: Verify OpenAPI Definition
#    needs: install-dependencies
#    uses: vanerac/dashboard-v2/.github/workflows/validate-swagger.yml
#  check-types:
#    name: Verify Compilation
#    needs: install-dependencies
#    uses: vanerac/dashboard-v2/.github/workflows/compile-ts.yml
